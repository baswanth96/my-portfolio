# Copy to `.env.local` for local overrides (never commit real secrets).

# Canonical site URL (metadata, OG tags, sitemap — use your production URL when deployed).
NEXT_PUBLIC_SITE_URL=https://www.baswanthkollu.me

# Optional: analytics or CMS keys (add as you integrate services).
# NEXT_PUBLIC_ANALYTICS_ID=
# CMS_API_TOKEN=
